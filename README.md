# assembly calculator

a simple calculator made in x86-64 assembly using nasm.

it supports:

- addition
- subtraction
- multiplication
- division

## requirements

You need:

- nasm
- gcc / mingw
- windows 64-bit

make sure both commands work in CMD:

```bash
nasm --version
gcc --version
